<Window x:Class="OpenWispr.Windows.UI.ModelDownloadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OpenWispr — Download Model" Width="420" Height="280"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <StackPanel Margin="24">
        <TextBlock Text="Choose a Whisper model" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
        <TextBlock Text="The model runs 100% locally on your PC. No internet required after download."
                   TextWrapping="Wrap" Foreground="#888" Margin="0,0,0,16"/>
        <ComboBox x:Name="ModelPicker" Margin="0,0,0,4" SelectionChanged="ModelPicker_SelectionChanged"/>
        <TextBlock x:Name="SizeLabel" Foreground="#888" Margin="0,0,0,16" FontSize="12"/>
        <ProgressBar x:Name="Progress" Height="8" Minimum="0" Maximum="1"
                     Visibility="Collapsed" Margin="0,0,0,8"/>
        <TextBlock x:Name="StatusLabel" Foreground="#888" Margin="0,0,0,16" FontSize="12"/>
        <Button x:Name="DownloadBtn" Content="Download" Click="DownloadBtn_Click"
                HorizontalAlignment="Left" Padding="20,8"/>
    </StackPanel>
</Window>
