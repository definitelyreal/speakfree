# Session handoff — 2026-06-03

Previous session ran extremely long; this is the clean kickoff for the next one.

## Quick state

**Shipped:**
- `main` branch is at `40b21e9` — v1.2.12 live for users (auto-update working since v1.2.11).
- Website `docs/index.html` points at v1.2.12 DMG with v1.2.11 + v1.2.12 changelog entries.

**In-flight on `wave-1` branch (commit `3136015`):**
- Task 1 of Wave 1 done and committed at `820e8a4` — `TextPipeline` core extracted from `AppDelegate`'s inline prompt-assembly closure. App and tests both call it. 8 new TextPipelineTests; corpus 22/22 still green.
- Wave 1 was re-merged with main today to pick up v1.2.11/v1.2.12 + website updates.
- `tasks.json` correctly shows Task 1 `"completed"` with commit SHA + review trail.
- Polish notes from the code-quality reviewer captured at [`docs/plans/2026-05-23-speakfree-2.0-wave-1-polish-notes.md`](2026-05-23-speakfree-2.0-wave-1-polish-notes.md) — Task 2 should address them (Input defaults, double-call elimination, `makeInput("")` placeholder).

## Reorder confirmed with Michael (2026-06-03)

Original Wave 1 → 2 → 3 → 2.1-cascade plan was REORDERED:

1. **Wave 1 (1.3.0)** — proceed as planned (Task 2–20). Foundation + Whisper crash fixes. ← *next*
2. **2.0 = Cascade Tier 1 reasoner** (pulled forward from 2.1). The actual user-visible quality jump. Macros 26 Foundation Models, "am I mid-sentence?" classifier, fixes homophones + filler-artifacts + comma-before-name. **Has no design doc yet — only brainstorm-level material in `build/26-05-17-tune-corpus/BRAINSTORM-SYNTHESIS.md`.**
3. **2.1** — insertion re-architecture (was Wave 2). Defer because it doesn't move text quality.
4. **2.2** — glossary v2 (was Wave 3).

## Two parallel kickoffs for the next session

### Track 1 (primary): execute Wave 1
```
git checkout wave-1   # if not already
/superpowers-extended-cc:executing-plans docs/plans/2026-05-23-speakfree-2.0-wave-1.md
```
The skill reads `tasks.json`, sees Task 1 completed, resumes from Task 2. Tasks 2 and 3 are unblocked first.

### Track 2 (parallel, low priority): write the Cascade Tier 1 design doc
Can happen in another fresh session in parallel — Wave 1 doesn't depend on Tier 1's design being locked, only on the integration point being defined. Inputs:
- `build/26-05-17-tune-corpus/BRAINSTORM-SYNTHESIS.md` — brainstorm-level reasoning
- `build/26-05-17-tune-corpus/RESEARCH.md` — research on competitors / phonetic detection / IMK
- The May 27 + Jun 2 failure data in `build/26-05-17-tune-corpus/` and ad-hoc evidence from `jun-2-recordings.txt`
- The constraints we locked: fully local + ephemeral + provenance-gated + 200-300ms tail-latency cap + constrained-chooser (not free-rewriter) + macOS 26 Foundation Models
- Save to `docs/plans/2026-06-XX-cascade-tier-1-design.md`, then `writing-plans` skill to produce a tasks list.

## What the new session does NOT need to do

- No more branch housekeeping — `wave-1` is current with main.
- No need to rerun ground-truth on June 2 recordings — `.gt.txt` files already produced.
- No publish step until Tasks 17–19 of Wave 1.

## State of the repo (June 3)

| Branch | Tip | Purpose |
|---|---|---|
| `main` | `40b21e9` | shipped v1.2.12; website updated |
| `wave-1` | `3136015` | merged with main; Task 1 done; awaiting Tasks 2–20 |
| `hotfix/1.2.11`, `hotfix/1.2.12` | merged to main already | safe to delete after v1.3.0 ships |

## Reading order for the new session

1. [`docs/plans/2026-05-23-speakfree-2.0-design.md`](2026-05-23-speakfree-2.0-design.md) — overall 2.0 design.
2. [`docs/plans/2026-05-23-speakfree-2.0-wave-1.md`](2026-05-23-speakfree-2.0-wave-1.md) — Wave 1 plan (20 tasks).
3. [`docs/plans/2026-05-23-speakfree-2.0-wave-1-polish-notes.md`](2026-05-23-speakfree-2.0-wave-1-polish-notes.md) — code-review notes from Task 1.
4. `tasks.json` (auto-loaded by executing-plans).

---
_Claude · 2026-06-03 · Session: 973aecd9-afee-4b85-a2c5-630e3eaa3c0a_
